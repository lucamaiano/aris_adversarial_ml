# Required to make the "experiments" dir the default one for the output of the models
hydra:
  job:
    chdir: true
  run:
    dir: ./logs/${model.name}/${now:%Y-%m-%d}/${now:%H-%M-%S}

# Default config for model and data
defaults:
  - _self_
  - model: pretrained
  - dataset: mnist
  - attack: fgsm

project: 'aris'